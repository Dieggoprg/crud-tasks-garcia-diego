import { Router } from "express";

export const router = Router();

router.post("/users",)

router.get("/users", () => {
})

router.get("/users", () => {
})

router.put("/users", () => {
})

router.delete("/users", () => {
})
